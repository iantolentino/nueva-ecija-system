# CONTINUE PROMPT — NUEVA ECIJA PORTAL

Paste this at the start of a new session to resume the Nueva Ecija Portal project.

---

Read the following files in this exact order before doing anything else:

1. `_brain/claude.md`
2. `_brain/summaries/session_handoff_2026-07-27.md`
3. `_brain/progress/progress.md`
4. `_brain/summaries/current_state.md`

After reading them, confirm briefly:

- the current project state
- the latest completed task/commit if available
- any held or blocked modules
- the next safest task to work on

Then wait for my instruction before modifying files.

Rules:

- Do not scan the whole repository unless the next task requires it.
- Do not guess from memory; use the four files above as the starting context.
- Preserve existing functionality unless I explicitly ask for a change.
- For bug fixes, read `_brain/fixes/fix_log.md` before changing code.
- For UI/design work, use the available UI/design skill if relevant.
- Update `_brain/progress/progress.md` and `_brain/summaries/current_state.md` after meaningful work.
- Commit and push completed work when I ask, or when the change is clearly complete and should be available across devices.

Important current context:

- Public/admin split exists.
- Latest UI direction is minimalist, based on `ui-ux-pro-max`.
- Vital Events has been removed.
- MTOP, QR Passes, and Job Matches are held pending confirmation.
- Test admin account: `dev-admin@ecija.gov` / `dev-admin2026!`

Do not start implementation immediately. Read, confirm, and wait.
