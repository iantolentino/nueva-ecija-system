{
  "name": "ecija-population-engine",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/check-local-env.js && vercel dev --listen 3000",
    "preview": "npm run dev",
    "check:local": "node scripts/check-local-env.js",
    "seed:demo": "node scripts/seed-demo-data.js",
    "seed:demo:reset": "node scripts/seed-demo-data.js --reset",
    "start": "node server.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0",
    "bcryptjs": "^2.4.3",
    "cookie": "^0.6.0",
    "uuid": "^9.0.1"
  }
}
